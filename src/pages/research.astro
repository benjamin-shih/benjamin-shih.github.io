---
import BaseLayout from "../layouts/BaseLayout.astro";
import { profile, publications, researchHistory, talks, thesis } from "../data/profile";
---

<BaseLayout title="Research | Benjamin Shih" description="Research interests, publications, thesis, and talks by Benjamin Shih.">
  <header class="page-heading">
    <h1>Research</h1>
    <p>
      My research interests lie in scientific and theoretical machine learning, approximation theory, and applications.
    </p>
  </header>

  <section>
    <h2>Publications</h2>
    {publications.map((paper) => (
      <article class="publication">
        <p>
          <strong>{paper.authors}</strong>, “{paper.title},” <em>{paper.venue}</em>, {paper.year}.
        </p>
        <p class="inline-links">
          {paper.links.map((link, index) => (
            <>
              <a href={link.href}>{link.label}</a>{index < paper.links.length - 1 && <span> · </span>}
            </>
          ))}
        </p>
      </article>
    ))}
  </section>

  <section>
    <h2>Bachelor's thesis</h2>
    <article class="publication">
      <p>
        <strong>{thesis.authors}</strong>, “{thesis.title},” {thesis.venue}, {thesis.year}.
      </p>
      <p class="inline-links">
        {thesis.links.map((link, index) => (
          <>
            <a href={link.href}>{link.label}</a>{index < thesis.links.length - 1 && <span> · </span>}
          </>
        ))}
      </p>
    </article>
  </section>

  <section>
    <h2>Research experience</h2>
    {researchHistory.map((item) => (
      <article class="plain-entry">
        <h3>{item.label}</h3>
        <p>{item.description}</p>
        <p class="inline-links">
          {item.links.map((link, index) => (
            <>
              <a href={link.href}>{link.label}</a>{index < item.links.length - 1 && <span> · </span>}
            </>
          ))}
        </p>
      </article>
    ))}
  </section>

  <section>
    <h2>Talks</h2>
    <ul>
      {talks.map((talk) => <li>{talk.title}, {talk.venue}, {talk.year}.</li>)}
    </ul>
  </section>

  <section>
    <h2>Contact</h2>
    <p>
      Questions about research or collaboration can be sent to <a href={`mailto:${profile.email}`}>{profile.email}</a>.
    </p>
  </section>
</BaseLayout>
