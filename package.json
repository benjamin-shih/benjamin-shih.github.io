{
  "name": "benjamin-shih-personal-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:dist": "node scripts/check-dist.mjs",
    "validate": "npm run build && npm run check:dist"
  },
  "devDependencies": {
    "astro": "^6.2.2"
  }
}
